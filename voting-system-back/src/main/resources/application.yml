spring:
  application:
    name: voting-system-back

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/db-vs-lifters
    username: lifters
    password: lifters

  liquibase:
    change-log: classpath:/db/changelog/changelog-master.yml
